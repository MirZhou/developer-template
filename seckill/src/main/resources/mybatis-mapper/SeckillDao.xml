<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mir.seckill.dao.SeckillDao">
    <!-- 减库存 -->
    <update id="reduceNumber">
        update
          db_seckill.seckill_product
        set
          number = number -1
        where
          seckill_id = #{seckillId}
          and start_time <![CDATA[ <= ]]> #{killTime}
          and end_time >= #{killTime}
          and number > 0
    </update>

    <!-- 根据ID查询秒杀对象 -->
    <select id="getById" resultType="Seckill" parameterType="long">
        select * from db_seckill.seckill_product where seckill_id = #{seckillId}
    </select>

    <select id="findAll" resultType="Seckill">
        select * from db_seckill.seckill_product order by create_time limit #{offset}, #{limit}
    </select>

    <select id="killByProcedure" statementType="CALLABLE">
        call execute_seckill(
          #{seckillId, jdbcType=BIGINT, mode = IN},
          #{userPhone, jdbcType=VARCHAR, mode = IN},
          #{killTime, jdbcType=DATE, mode = IN},
          #{result, jdbcType=INTEGER, mode = OUT}
        );
    </select>
</mapper>